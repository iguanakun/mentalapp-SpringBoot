<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mentalapp.cbt_basic.mapper.CbtBasicsMapper">

    <!-- 全件取得 -->
    <select id="selectAll" resultType="CbtBasics">
        select * from cbt_basics
    </select>

    <!-- １件取得 -->
    <select id="selectByPrimaryKey" resultType="CbtBasics">
        select * from cbt_basics
        where id = #{id}
    </select>
    
    <!-- ユーザーIDによる取得 -->
    <select id="selectByUserId" resultType="CbtBasics">
        select * from cbt_basics
        where user_id = #{userId}
    </select>

    <!-- 登録 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into cbt_basics(fact, mind, body, behavior, user_id, created_at, updated_at)
        values(#{fact}, #{mind}, #{body}, #{behavior}, #{user.id}, #{createdAt}, #{updatedAt})
    </insert>

    <!-- 更新 -->
    <update id="updateByPrimaryKey">
        update cbt_basics
        set fact = #{fact},
            mind = #{mind},
            body = #{body},
            behavior = #{behavior},
            updated_at = #{updatedAt}
        where id = #{id}
    </update>

    <!-- 削除 -->
    <delete id="deleteByPrimaryKey">
        delete from cbt_basics
        where id = #{id}
    </delete>

</mapper>
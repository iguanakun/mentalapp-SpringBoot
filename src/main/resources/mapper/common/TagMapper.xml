<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mentalapp.common.dao.TagMapper">

    <!-- ==================== resultMap定義 ==================== -->

    <!-- Tag -->
    <resultMap id="TagResultMap" type="com.mentalapp.common.entity.Tag">
        <id column="id" property="id"/>
        <result column="tag_name" property="tagName"/>
        <result column="user_id" property="userId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- ==================== CRUD操作 ==================== -->

    <!-- 主キーによる取得 -->
    <select id="selectByPrimaryKey" resultMap="TagResultMap">
        SELECT
        id,
        tag_name,
        user_id,
        created_at,
        updated_at
        FROM tags
        WHERE id = #{id}
    </select>

    <!-- タグ名による取得 -->
    <select id="findByTagName" resultMap="TagResultMap">
        SELECT
        id,
        tag_name,
        user_id,
        created_at,
        updated_at
        FROM tags
        WHERE tag_name = #{tagName}
    </select>

    <!-- ユーザーIDによる取得 -->
    <select id="findByUserId" resultMap="TagResultMap">
        SELECT
        id,
        tag_name,
        user_id,
        created_at,
        updated_at
        FROM tags
        WHERE user_id = #{userId}
    </select>

    <!-- タグ名とユーザーIDによる取得 -->
    <select id="findByTagNameAndUserId" resultMap="TagResultMap">
        SELECT
        id,
        tag_name,
        user_id,
        created_at,
        updated_at
        FROM tags
        WHERE user_id = #{userId}
        AND tag_name = #{tagName}
    </select>

    <!-- 全件取得 -->
    <select id="findAll" resultMap="TagResultMap">
        SELECT
        id,
        tag_name,
        user_id,
        created_at,
        updated_at
        FROM tags
    </select>

    <!-- モニタリングIDに紐づくタグを取得 -->
    <select id="findByMonitoringId" resultMap="TagResultMap">
        SELECT
        t.id,
        t.tag_name,
        t.user_id,
        t.created_at,
        t.updated_at
        FROM tags t
        INNER JOIN ${tableName} rel ON t.id = rel.tag_id
        WHERE rel.${columnName} = #{monitoringId}
    </select>

    <!-- 登録 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tags (
        tag_name, user_id, created_at, updated_at
        ) VALUES (
        #{tagName}, #{userId}, current_timestamp, current_timestamp
        )
    </insert>

    <!-- 更新 -->
    <update id="updateByPrimaryKey">
        UPDATE tags
        SET tag_name = #{tagName},
        user_id = #{userId},
        updated_at = current_timestamp
        WHERE id = #{id}
    </update>

    <!-- 削除 -->
    <delete id="deleteByPrimaryKey">
        DELETE FROM tags
        WHERE id = #{id}
    </delete>

</mapper>

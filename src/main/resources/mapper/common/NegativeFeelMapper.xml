<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mentalapp.common.dao.NegativeFeelMapper">

    <!-- 全件取得 -->
    <select id="selectAll" resultType="NegativeFeel">
        SELECT
        id,
        negative_feel_name,
        created_at,
        updated_at
        FROM negative_feels
    </select>

    <!-- １件取得 -->
    <select id="selectByPrimaryKey" resultType="NegativeFeel">
        SELECT
        id,
        negative_feel_name,
        created_at,
        updated_at
        FROM negative_feels
        WHERE id = #{id}
    </select>

    <!-- 名前による取得 -->
    <select id="selectByName" resultType="NegativeFeel">
        SELECT
        id,
        negative_feel_name,
        created_at,
        updated_at
        FROM negative_feels
        WHERE negative_feel_name = #{negativeFeelName}
    </select>

    <!-- 登録 -->
    <insert id="insert">
        INSERT INTO negative_feels (
        negative_feel_name, created_at, updated_at
        ) VALUES (
        #{negativeFeelName}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <!-- 更新 -->
    <update id="updateByPrimaryKey">
        UPDATE negative_feels
        SET negative_feel_name = #{negativeFeelName},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <!-- 削除 -->
    <delete id="deleteByPrimaryKey">
        DELETE FROM negative_feels
        WHERE id = #{id}
    </delete>

</mapper>
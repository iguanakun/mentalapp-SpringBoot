<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mentalapp.common.dao.PositiveFeelMapper">

    <!-- 全件取得 -->
    <select id="selectAll" resultType="PositiveFeel">
        SELECT
        id,
        positive_feel_name,
        created_at,
        updated_at
        FROM positive_feels
    </select>

    <!-- １件取得 -->
    <select id="selectByPrimaryKey" resultType="PositiveFeel">
        SELECT
        id,
        positive_feel_name,
        created_at,
        updated_at
        FROM positive_feels
        WHERE id = #{id}
    </select>

    <!-- 名前による取得 -->
    <select id="selectByName" resultType="PositiveFeel">
        SELECT
        id,
        positive_feel_name,
        created_at,
        updated_at
        FROM positive_feels
        WHERE positive_feel_name = #{positiveFeelName}
    </select>

    <!-- 登録 -->
    <insert id="insert">
        INSERT INTO positive_feels (
        positive_feel_name, created_at, updated_at
        ) VALUES (
        #{positiveFeelName}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <!-- 更新 -->
    <update id="updateByPrimaryKey">
        UPDATE positive_feels
        SET positive_feel_name = #{positiveFeelName},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <!-- 削除 -->
    <delete id="deleteByPrimaryKey">
        DELETE FROM positive_feels
        WHERE id = #{id}
    </delete>

</mapper>